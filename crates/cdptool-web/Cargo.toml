[package]
name = "cdptool-web"
version = "0.2.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
cdptool = { path = "../cdptool" }
wasm-bindgen = "0.2"
js-sys = "0.3"
